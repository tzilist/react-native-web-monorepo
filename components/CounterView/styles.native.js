import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'row',
    marginTop: 100,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    width: '20%',
    textAlign: 'center',
  },
});
